Crie uma função que dado um número inteiro n retorna o somatorio dos números de 0 a n


definir uma função "Teste" que recebe um numero n como parametro 
criar uma variavel com valor de inicio, para calcular a somatória dos numeros da lista.

criar um laço de loop for onde i é uma variavel interavel representando o indice passando pelo parametro informado


usar variavel acumuladora soma onde soma vai ser igual a soma mais i

retornar soma 

def somalista(numeros):
    soma = 0
    for i in numeros:
        soma = soma + i
    return soma

print(somalista([1,3,5,7,9]))
